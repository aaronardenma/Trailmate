version: "3.8"
services:
#  mongodb:
#    image: mongo:latest
#    container_name: mongodb
#    ports:
#      - "27017:27017"
#    volumes:
#      - mongo-data:/data/db
  backend:
    build:
      context: backend
      dockerfile: ../Dockerfile.api
    ports:
      - "5001:5001"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
  frontend:
    build:
      context: frontend
      dockerfile: ../Dockerfile.ui
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
#volumes:
#  mongo-data:
